[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ryuu-knowledge-base"
version = "0.2.0a1"
description = "RYUU Knowledge Base — IKnowledgeBackbone protocol and shared models"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/ryuu_knowledge_base"]
