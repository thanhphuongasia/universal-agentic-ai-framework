[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ryuu-storage-sqlite"
version = "0.3.0a1"
description = "SQLite IKVStore + ICollectionStore — stdlib sqlite3 wrapped in asyncio.to_thread."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "ryuu-storage-core>=0.3.0a1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "anyio[trio]>=4.0",
    "pytest-cov>=4.0",
    "mypy>=1.0",
    "ruff>=0.6.0",
]

[project.urls]
Repository = "https://github.com/core-corp/ryuu-framework"

[tool.hatch.build.targets.wheel]
packages = ["src/ryuu_storage_sqlite"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""
