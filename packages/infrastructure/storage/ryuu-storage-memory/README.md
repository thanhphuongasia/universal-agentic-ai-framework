# ryuu-storage-memory

In-memory `IKVStore` + `ICollectionStore` implementations. Zero external dependencies. Wipes on process restart.

Use this for tests, notebooks, and prototyping. Swap for `ryuu-storage-sqlite` (or another backend) when persistence matters.
