"""Code-analysis investigator plugin — domain-specific for CRUD matrix + Spring + Neo4j.

NOT a generic framework — uses ryuu-eval-investigator core + domain rules here.
Runs locally for debug; not deployed to production prod-grade-code-analysis server.
"""
